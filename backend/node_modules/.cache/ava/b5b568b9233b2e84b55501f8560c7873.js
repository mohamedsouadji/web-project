'use strict';

var pgp = require('pg-promise')();
var path = require('path');
var QueryFile = require('pg-promise').QueryFile;

var config = {
  host: 'localhost',
  port: 5432,
  database: 'efrei',
  user: 'postgres',
  password: 'efrei'
};

const DB = {
  accessor: pgp(config),

  sql: function (file) {
    var fullPath = path.join(__dirname, file); // generating full path;
    return new QueryFile(fullPath, { minify: false });
  }

};

module.exports = DB;
//# sourceMappingURL=../node_modules/.cache/ava/b5b568b9233b2e84b55501f8560c7873.js.map