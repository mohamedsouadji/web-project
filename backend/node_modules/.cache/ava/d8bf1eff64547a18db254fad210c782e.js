'use strict';

var express = require('express');
var router = express.Router();
const charactersdao = require('../models/charactersdao');

/* GET users listing. */
router.get('/', function (req, res, next) {
  charactersdao.getAll().then(resultat => {
    res.send(result);
  });
});

module.exports = router;
//# sourceMappingURL=../node_modules/.cache/ava/d8bf1eff64547a18db254fad210c782e.js.map