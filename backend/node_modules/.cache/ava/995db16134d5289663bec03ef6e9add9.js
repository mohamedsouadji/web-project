'use strict';

var express = require('express');
var router = express.Router();
var usersdao = require('../models/usersdao.js');

/* GET users listing. */
router.get('/', function (req, res, next) {
	usersdao.getAll().then(result => {
		res.send(result);
		console.log(result);
	});
});

router.get('/:id', function (req, res, next) {
	usersdao.getTest(req.params.id).then(resultat => {
		console.log(resultat);
		res.status(200).json({
			parameters: {
				id: resultat[0].id
			},
			result: {
				status: "success",
				user: {
					id: resultat[0].id,
					name: resultat[0].name,
					email: resultat[0].email,
					alliance_id: resultat[0].alliance_id
				}
			}
		});
		//send(resultat)
	});
});
module.exports = router;
//# sourceMappingURL=../node_modules/.cache/ava/995db16134d5289663bec03ef6e9add9.js.map